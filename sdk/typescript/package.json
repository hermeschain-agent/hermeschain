{
  "name": "@hermeschain/sdk",
  "version": "0.1.0",
  "description": "TypeScript SDK for Hermeschain — read state, submit txs, build VM programs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test tests/*.test.js",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "hermeschain",
    "blockchain",
    "sdk"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
