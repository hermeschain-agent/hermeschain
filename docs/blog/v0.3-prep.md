# Hermeschain v0.3 (preview)

Tier-3 protocol updates landing across the next ~37 hours via the autonomous pacer:

## What's new
- Multi-validator consensus with rotating producers + 2/3 quorum
- Network peer mesh with /api/mesh/* endpoints + bootstrap heartbeat
- Real VM with dynamic gas + receipt logs (PUSH/POP/ADD/SUB/MUL/DIV/MOD/EQ/LT/GT/AND/OR/NOT/SSTORE/LOG/STOP/REVERT)
- CIMonitor with 5s file-watch debounce
- Complete observability: Prometheus metrics, structured access log, slow-query log, three-tier health, request IDs
- Token economics: env-tunable block reward, 80/20 fee split (planned), burn counter, finalized block flag
- 30+ DB migrations covering uncles, slashing, contract code/storage/metadata, snapshots, peers, newsletter, scheduled txs, gas caps, session keys, threat blocklist, suspicious events, …
- TypeScript SDK skeleton + counter/erc20-like/multisig/oracle/vrf example contracts
- Complete docs reorg: per-endpoint refs, per-opcode refs, runbooks, threat model, bug bounty

Versioned release tag once main catches up to tier-3-backlog HEAD.
