# Code style

- TypeScript strict mode
- 2-space indent (Prettier)
- Single quotes, trailing comma all
- Async/await over promise chains
- Avoid `any` (use `unknown` + narrow)
- Imports grouped: external, then internal, then types
- Named exports preferred; default exports for React components only
