# Migration from v0.1 to v0.2

## Breaking changes
None — v0.2 was a feature addition release.

## New features
- Reorg actually works (state revert, mempool eviction, ForkManager wired into Chain.addBlock)
- Persistent receipts via DB
- Tx hash + account alias endpoints
- DB migration runner

## Action required
None for clients. Backend operators: run migrations on cold boot.
