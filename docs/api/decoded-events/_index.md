# Decoded events index

When ?decodeLogs=true is passed to /api/tx/:hash, contracts with registered ABI (contract_metadata table, TASK-097) get their logs decoded.

Per-event reference docs in this directory cover the standard event signatures Hermeschain ships with.
