# Event: Transfer

Standard event signature topic-0: `keccak256(\"Transfer(...)\")[0..32]`

## Fields

Defined per contract via the ABI registry (TASK-097). Decoded form returned when ?decodeLogs=true.
