# Script: migrate-status

`backend/scripts/migrate-status.js` (or .ts)

## Purpose
Lists applied + pending migrations. --dry-run prints SQL each pending would execute.

## Invocation
`npm run migrate:status`
