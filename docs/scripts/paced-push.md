# Script: paced-push

`backend/scripts/paced-push.js` (or .ts)

## Purpose
Drains tier-3-backlog → main one commit per fire. Cadence configured externally (cron / worker interval).

## Invocation
`npm run paced:push`
