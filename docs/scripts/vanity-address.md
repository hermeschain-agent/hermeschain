# Script: vanity-address

`backend/scripts/vanity-address.js` (or .ts)

## Purpose
Brute-force keypair gen until publicKey matches --prefix. CLI: --prefix --max --starts-with.

## Invocation
`npm run vanity:address`
