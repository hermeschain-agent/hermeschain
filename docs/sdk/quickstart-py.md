# SDK Quickstart (Python — planned)

```bash
pip install hermeschain
```

```python
from hermeschain import Client
c = Client()
print(c.status())
print(c.get_balance('your-addr'))
```

Python SDK package skeleton lands in TASK-278.
