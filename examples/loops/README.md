# Loop pattern

Demonstrates the structure for a counted loop. Real loops require JUMP + JUMPI (TASK-066) to repeat. This example linearizes one iteration; once jump opcodes ship, the LoopBody section can repeat conditionally.
