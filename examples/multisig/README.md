# Multi-sig example (skeleton)

Demonstrates proposal counter increment + Proposed event. Full m-of-n confirmation flow requires CALL/RETURN (TASK-070, TASK-071) which is not yet shipped.

Use this as the bones; layer on confirmation logic once cross-contract calls land.
