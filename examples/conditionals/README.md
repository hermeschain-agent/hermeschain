# Conditional pattern

Push two values, compare, branch logic. Real branching requires JUMPI (TASK-066). This example shows the comparison + log emission pattern that compiles to branches once jumps land.
