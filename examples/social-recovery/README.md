# Social recovery example (skeleton)

3-of-5 guardian threshold check. Real flow needs ECRECOVER (TASK-078) + CALL (TASK-070) so guardians can sign + the recovery contract can verify them. This stub shows the threshold check structure.
