{
  "name": "hermes-wallet-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Self-custody browser-extension wallet for Hermeschain. Your keys never leave the device.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test src/crypto/*.test.ts && node --import tsx --test src/wallet/*.test.ts"
  },
  "dependencies": {
    "@noble/ed25519": "^2.1.0",
    "@noble/hashes": "^1.5.0",
    "@scure/bip39": "^1.4.0",
    "bs58": "^6.0.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.279",
    "esbuild": "^0.24.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
